{
  "permissions": {
    "allow": [
      "Bash(git add *)",
      "Bash(git commit *)",
      "Bash(git status)",
      "Bash(git log *)",
      "Bash(git diff *)",
      "Bash(git branch *)",
      "Bash(git checkout *)",
      "Bash(git pull)",
      "Bash(git fetch *)",
      "Bash(git stash *)",
      "Bash(git merge *)",
      "Bash(gh *)",
      "Bash(ls *)",
      "Bash(pwd)",
      "Bash(wc *)",
      "Bash(cp *)",
      "Bash(mv *)",
      "Bash(mkdir *)",
      "Bash(touch *)",
      "Bash(node *)",
      "Bash(npm *)",
      "Bash(npx *)",
      "Bash(pnpm *)"
    ],
    "deny": [
      "Bash(git push --force *)",
      "Bash(git reset --hard *)",
      "Bash(rm -rf *)",
      "Bash(sudo *)",
      "Bash(curl * | sh)",
      "Bash(curl * | bash)"
    ]
  },
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "echo '📍 Recent commits:' && git log --oneline -5 2>/dev/null || true"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "FILE=$(cat | jq -r '.tool_input.file_path // empty'); if [[ -n \"$FILE\" && \"$FILE\" == *settings.json && \"$FILE\" != *settings.local.json ]]; then echo '{\"decision\": \"block\", \"reason\": \"Cannot modify shared project settings.json directly. Edit settings.local.json for local overrides or ask the user to update settings.json.\"}'; else echo '{\"decision\": \"approve\"}'; fi",
            "timeout": 5
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "FILE=$(cat | jq -r '.tool_input.file_path // empty'); [[ -n \"$FILE\" && \"$FILE\" =~ \\.(ts|tsx|js|jsx|json)$ ]] && npx prettier --write \"$FILE\" 2>/dev/null || true",
            "timeout": 10
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "prompt",
            "prompt": "Check if there are uncommitted changes in the working tree (staged or unstaged). If there are meaningful uncommitted changes, reply {\"ok\": false, \"reason\": \"There are uncommitted changes — consider committing before ending the session.\"}. If everything is committed or changes are trivial (e.g. only settings.local.json), reply {\"ok\": true}."
          }
        ]
      }
    ]
  }
}
