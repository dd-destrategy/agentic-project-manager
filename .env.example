# =============================================================================
# Agentic PM Workbench — Environment Variables
# =============================================================================
# Copy this file to .env.local and fill in the values:
#   cp .env.example .env.local
#
# For local development with LocalStack, most defaults below work as-is.
# You only MUST provide: NEXTAUTH_SECRET, NEXTAUTH_PASSWORD, ANTHROPIC_API_KEY
# =============================================================================

# --- AWS Configuration -------------------------------------------------------
AWS_REGION=ap-southeast-2
TABLE_NAME=AgenticPM
TABLE_ARN=arn:aws:dynamodb:ap-southeast-2:000000000000:table/AgenticPM

# --- Authentication ----------------------------------------------------------
# Generate: openssl rand -base64 32
NEXTAUTH_SECRET=CHANGE-ME-generate-with-openssl-rand-base64-32
# Generate: node -e "console.log(require('bcryptjs').hashSync('your-password', 10))"
NEXTAUTH_PASSWORD=CHANGE-ME-bcrypt-hash-here
NEXTAUTH_URL=http://localhost:3000

# --- Claude API --------------------------------------------------------------
ANTHROPIC_API_KEY=sk-ant-api03-CHANGE-ME

# --- Email & Notifications ---------------------------------------------------
ALERT_EMAIL=admin@example.com
SES_FROM_ADDRESS=pm-agent@example.com
SES_VERIFIED_DOMAIN=example.com
DIGEST_EMAIL=admin@example.com

# --- Application Configuration -----------------------------------------------
ENVIRONMENT=dev
NODE_ENV=development
LOG_LEVEL=debug

# --- Local Development (LocalStack) ------------------------------------------
DYNAMODB_ENDPOINT=http://127.0.0.1:4566
LOCALSTACK_ENDPOINT=http://127.0.0.1:4566

# --- CDK Deployment (only needed for AWS deploy) -----------------------------
# CDK_DEFAULT_ACCOUNT=123456789012
# CDK_DEFAULT_REGION=ap-southeast-2
